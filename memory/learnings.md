# Aprendizados do Projeto GastroMundi

## Objetivo
Registrar aprendizados obtidos ao longo do projeto — erros cometidos, surpresas, insights e lições que a equipe não quer repetir ou quer replicar. É a memória viva do que funcionou e do que não funcionou.

## Contexto
Aprendizados não documentados se perdem. Este arquivo captura o conhecimento tácito que normalmente fica na cabeça das pessoas e some quando elas saem da equipe.

## Regras Gerais
- Qualquer membro da equipe pode (e deve) registrar aprendizados
- Aprendizados negativos (erros, falhas) devem ser registrados sem julgamento ou culpa
- Aprendizados que geram padrão devem ser movidos para `memory/patterns.md`

## Validações
- Aprendizados técnicos críticos devem ser validados por, no mínimo, um segundo membro
- Aprendizados relacionados a incidentes devem referenciar o post-mortem correspondente

## Permissões
- Todos os membros da equipe têm permissão de leitura e escrita neste arquivo

## Exceções
- Aprendizados que envolvam dados sensíveis de usuários devem ser anonimizados antes do registro

## Auditoria
- Data e autor de cada aprendizado devem ser registrados
- Revisão periódica recomendada: ao final de cada sprint ou ciclo

## Eventos
- `learning.added` — novo aprendizado registrado
- `learning.promoted` — aprendizado elevado a padrão oficial

## Configurações Futuras
- Criar ritual de retrospectiva com base neste arquivo
- Integrar aprendizados ao processo de onboarding

## Casos de Uso
- Retrospectivas de sprint
- Onboarding de novos membros
- Análise pós-incidente
- Planejamento de arquitetura futura

## Critérios de Aceite
- [ ] Cada aprendizado tem data, autor, contexto e lição clara
- [ ] Aprendizados estão categorizados por área
- [ ] Aprendizados promovidos a padrões estão referenciados

---

> Este arquivo é uma memória viva: a maioria dos aprendizados surge ao longo da execução. As entradas abaixo registram aprendizados da fase de fundação (Fase 0) e servem de modelo para os próximos.

## Aprendizados Técnicos

| Data | Aprendizado | Lição / Ação |
|------|-------------|--------------|
| 2026-06 | Isolar acesso ao Supabase em uma camada de serviços desde o início evita refatoração dolorosa depois. | Manter chamadas ao backend fora dos componentes (ver `memory/patterns.md` e decisão 007). |
| 2026-06 | RLS é poderoso, mas só protege se estiver ativo em **todas** as tabelas — uma tabela esquecida vira brecha. | Tratar RLS como requisito de definição de pronto para qualquer tabela nova. |
| 2026-08-01 | `src/lib/deliveryHorarioSqlGuard.test.js` proibia o token `lpad(` e **exigia** `'FM000'` no número do pedido do delivery. Como `to_char(n,'FM000')` não expande o gabarito (o que não cabe vira `###`), o guard obrigava a reintroduzir o bug D14 que ele existia para impedir: corrigir a migration deixou a suíte vermelha. | Guard de SQL proíbe a **forma** defeituosa (`lpad(x, 3, '0')`), nunca a palavra — e prova a regex nos dois lados, com um caso que ela deve pegar e um que ela não pode pegar. Ver `memory/patterns.md` → "Conferência textual de SQL". |
| 2026-08-01 | `ConfirmarRenovacaoModal.test.jsx` e `PlanosDashboard.test.jsx` diziam reproduzir "as recusas reais" da RPC `confirmar_renovacao_assinatura`, mas as frases eram inventadas e o código era `P0002`. Como o modal só repassa `error.message` sem interpretar, a suíte ficou verde documentando uma tela que ninguém nunca veria. | Teste que dubla erro de RPC copia a frase **e** o SQLSTATE verbatim da migration — mock inventado passa sempre e não prova nada. E `RAISE EXCEPTION` sem `USING ERRCODE` chega como **`P0001`** (`raise_exception`): só quem declara `ERRCODE` devolve `42501`/`23505`. |
| 2026-08-01 | O TD012 estava marcado como "engole a exceção" e já tinha sido metade consertado meses antes: a falha era desfeita na tela, mandada ao Sentry, ao `jarvas_eventos` e ao `activity_log`. Três destinos, e nenhum deles é uma tela que o dono de restaurante abre — o furo de inventário continuava invisível para a única pessoa que podia agir. | Reportar não é alertar. Antes de fechar qualquer item de "falha silenciosa", perguntar **em qual tela do usuário isso aparece** — se a resposta é Sentry, tabela de evento ou log, o item não está feito. No GastroMundi a superfície do gestor é o painel do Jarvas (`jarvas_insights`). |
| 2026-08-01 | O teste em `estoque.test.js` afirmava `expect(quantidade).toBe(3)` com o comentário `// fallback calculado localmente` — ou seja, prendia por escrito o saldo inventado que era o próprio TD012. Verde há meses. | Item de débito velho: ler o teste do trecho antes de concluir que o defeito sumiu. Um teste pode estar guardando o bug em vez do comportamento, e a suíte verde não distingue os dois. |
| 2026-08-02 | O `KLogo.jsx` tinha `color: "#fff"` inline, e tirar o hex do JSX esbarrou num buraco do design system: **não existe token `--gm-*` para texto por cima da cor de marca**. A lista `TOKENS_PERMITIDOS` em `src/lib/tema.js` é fechada de propósito (accent, alow, bg, card, surface, border, green, red, blue, warn, text, muted, faint) e `--gm-bg`/`--gm-card` não servem: num tenant de tema escuro viram texto escuro sobre roxo. O `#fff` continuou literal, agora no `.css`, igual o `.btn--primario` do `vitrine.css` já fazia. | Antes de prometer "zero cor literal" num critério de aceite, conferir se o token existe. Cor de **contraste** (o que fica em cima do accent) é uma categoria que o tema por tenant ainda não modela — enquanto não houver decisão do dono, o literal fica no CSS com o motivo escrito ao lado, nunca no JSX. |
| 2026-08-02 | Recontei o progresso do F018 procurando um `.css` **irmão de mesmo nome** do `.jsx` e "corrigi" o backlog de 121 para 118 — errado. `PDVView/index.jsx` importa `./PDVView.css` (o CSS leva o nome do componente, não do arquivo `index`), e o script marcou o maior arquivo do projeto como "sem CSS". Contando pelo `import "./*.css"` real, o número original do backlog estava certo. | Progresso de refactor se mede pelo **import**, não pelo nome do arquivo ao lado: `find`+`-f "$base.css"` mente em toda pasta que usa `index.jsx`. E antes de publicar correção de número, rodar a contagem pelos dois critérios e ver se batem. |
| 2026-08-02 | A métrica que o F018 vinha citando ("121 dos 156 `.jsx` importam `.css` co-localizado") estava quase completa e mesmo assim o item continuava enorme: só **1** arquivo do projeto (`src/main.jsx`) tem estilo inline sem nenhum CSS próprio. A dívida real são **2068 `style={{` em 46 arquivos que JÁ importam seu `.css`** — `PDVView/index.jsx` (247), `DeliveryView.jsx` (217), `RelatorioView.jsx` (202), `NotasFiscaisTab.jsx` (195). | A métrica media a coisa errada: "tem arquivo `.css`" não é "o estilo saiu do JSX". Item transversal precisa de métrica que meça o **trabalho restante** (ocorrências de `style={{`), não a adoção do arquivo. Trocada na linha do F018. |
| 2026-08-02 | `src/components/shared/KLogo.jsx` não é importado por nenhum arquivo do repositório (`grep -rn "KLogo" src bot` só acha a própria definição). Foi migrado para o padrão do F018 sem que nada o exercite — a suíte não teria acusado se ele tivesse quebrado. | Componente compartilhado sem nenhum uso: registrar e devolver ao dono (apagar ou reusar), porque migrar código morto gasta rodada e não muda nada na tela. Antes de incluir um "shared" numa fatia de refactor, conferir quem o importa. |
| 2026-08-01 | A âncora `/'FM000'/` continuou passando na migration `20260907` depois do conserto — casava o comentário em português que explica a fórmula removida, não o código. Mesmo falso positivo que já tinha aparecido em `20260909` sobre `pg_get_functiondef()`. | Toda conferência textual de SQL corta comentário antes: `linha.replace(/--.*$/, "")` em JS, `regexp_replace(v_def, '--.*', '', 'gn')` em PL/pgSQL. Aconteceu duas vezes — virou padrão, não é coincidência. |
| 2026-08-01 | `salvarBrandingCache` carimba o cache com `resolverSlugTenant()` (`brandingCache.js:91`) — o slug da **origem**, não o da tela. Enquanto não há subdomínio por estabelecimento, isso é sempre `gastromundi`. Uma prévia da Casa Coffee que gravasse o cache faria a marca dela ser pintada na tela de login de todo mundo que abre a mesma origem. | Cache carimbado por origem só pode ser escrito por tela que **é** daquela origem. Toda tela que renderiza a marca de outro estabelecimento (prévia, console, demonstração) não lê nem grava o cache — ler pinta a marca errada no primeiro quadro, gravar vaza a marca para o vizinho. Ver `memory/patterns.md` → "Superfície pública endereçada por slug". |

| 2026-08-01 | A linha do F022 em `docs/09_BACKLOG/features.md` dizia, em nota, que "RLS por tenant precisa de override `OR auth.is_super_admin()`". A ADR-008 (§5 e decisão v2 nº 2) diz o contrário para tabela operacional: o ramo só existe em `tenants` e `assinaturas`. Quem fosse construir o analytics lendo o backlog iria direto alterar a policy de `vendas` e abrir a base inteira de todos os clientes a qualquer token de plataforma. | Nota de backlog envelhece e não é revisada quando a ADR muda. Antes de implementar um item, a fonte é a ADR — o backlog diz **o que** falta, nunca **como** se faz. A nota do F022 foi corrigida nesta rodada; a regra virou padrão em `memory/patterns.md` → "O Console lê a operação por agregado, nunca por policy". |
| 2026-08-01 | O bloco `DO $conf$` das migrations foi tratado até aqui como a proteção da RPC. Ele só vale no banco onde a migration já rodou — e desaparece junto com a função no primeiro `CREATE OR REPLACE`. Para a `analytics_plataforma`, o que precisa sobreviver é textual: a assinatura de retorno não pode ganhar coluna que identifique venda. | RPC cuja **forma** é a garantia de segurança (assinatura de retorno, ordem da guarda, `REVOKE` antes do `GRANT`) nasce com os dois: o autoteste em SQL, que falha alto no banco, e o `*SqlGuard.test.js`, que falha na suíte de quem editar o arquivo. Já são 9 guards; o padrão é esse. |
| 2026-08-01 | `estornoPagamentoSqlGuard.test.js` procurava `p_tenant_id`, `DELETE FROM public.assinaturas_pagamentos` e `TRUNCATE` no texto **inteiro** da `20260913` — e achou os três dentro do próprio `DO $conf$`, que cita essas palavras nos `LIKE` da conferência. O guarda acusou o vigia como se fosse o infrator: 3 falhas em código correto. | Guard textual proíbe só no que **é** a migration: `const migracao = sql.slice(0, sql.indexOf("DO $conf$"))`. O bloco de conferência necessariamente cita o que proíbe — varrer o arquivo todo garante falso positivo. Ver `memory/patterns.md` → "Conferência textual de SQL". |
| 2026-08-01 | No mesmo guard, `/FOR (INSERT\|UPDATE\|DELETE)/i` (para provar que nenhuma policy de escrita nasceu) casou o `... FOR UPDATE` dos locks de linha da própria RPC — a regex do jargão de policy é subconjunto do jargão de lock. E `recorte(cliente, ..., "}\n")` devolveu −1: os fontes deste repositório são gravados com **CRLF**, então âncora com `"\n"` nunca casa. | Para provar ausência de policy, ancore no que só a policy tem (`GRANT ... ON TABLE`, `CREATE POLICY`), nunca em `FOR <verbo>`. E todo `readFileSync` de guard normaliza antes de ancorar: `.replace(/\r/g, "")`. |

| 2026-08-01 | `AssinaturaBanner.jsx` declarava `const DIAS_AVISO_PRE_VENCIMENTO = 5` no próprio componente, e `rotuloCompetencia` morava dentro de `ConfirmarRenovacaoModal.jsx` (Console). A aba nova do estabelecimento precisava dos dois: copiar o `5` deixaria banner e aba discordando na primeira vez que alguém mudasse a janela ("está em dia" com o banner avisando que vence), e importar do Console amarraria a tela do tenant à superfície da plataforma que a decisão 027 mantém separada. | Constante de regra e função pura que **duas superfícies** leem moram em `src/lib` desde o começo — componente é dono de marcação, não de regra. O sintoma de que está no lugar errado é precisar importar um componente para reaproveitar uma linha. |
| 2026-08-01 | Na review da mesma aba, dois textos afirmavam o que a tela não sabia: com `tenant` ainda nulo no bootstrap ela escrevia "ainda não há uma assinatura cadastrada" (a frase que assusta justamente quem paga em dia), e sem resposta de `public.planos` caía no código cru `medio`. Pior: o teste que eu mesmo tinha escrito **ratificava** o jargão — `it("cai no código do plano…")` esperava `medio` na tela. | Antes de escrever frase categórica, perguntar "o dado que sustenta isso já chegou?". E teste de fallback descreve o que o usuário **deve** ler, não o que o código faz hoje — teste que congela o comportamento errado passa para sempre. Ver `memory/patterns.md` → "'Ainda não sei' nunca é dito como 'não existe'". |

| 2026-08-01 | A prévia da marca importou `logoUrlSegura` de `@/lib/tema` e quebrou o build da aba: tema.js **não** exporta esse nome. A allowlist de esquema vive lá como `logoUrlSeguraParaImg`, privada de propósito, e o `logoUrlSegura` exportado é outro, de `src/lib/impressao/renderizar.js` — que tema.js evita importar por ser carregado no bootstrap do app inteiro. Dois helpers com o mesmo papel e nomes quase iguais em módulos diferentes. | Antes de importar helper de segurança, conferir a linha de `export` do módulo — nome plausível não é nome existente. Para aplicar a allowlist em tela, o caminho público é `logoUrlTenant({ logo_url })`: é a mesma política, é o que o `SidebarBranding` usa, e a prévia passa a exercitar a defesa em vez de duplicá-la. |
| 2026-08-01 | `comprimirImagem` de `src/lib/deliveryFotos.js` quase foi reusado para o logo. Ele pinta `fillRect` branco no canvas e emite JPEG **de propósito** (foto de prato não tem alpha) — reusar poria um retângulo branco atrás da marca de todo estabelecimento, sobre a sidebar escura (`--gm-surface: #161b2c`). | Compressão de imagem não é uma função só: **foto** achata em JPEG com fundo, **logo** preserva alpha em PNG e nunca pinta fundo. Reuso vai só na matemática pura (`calcularDimensoes`), que é o que de fato não tem opinião sobre o conteúdo. O spec desta rodada dizia `logo.jpg` e foi corrigido em cinco pontos, não o código. |
| 2026-08-01 | O banner "Identidade salva" nunca acendia no teste da `IdentidadeTab`: a condição `ok && !mudou` comparava o formulário com o tema vindo do `AppContext`, que num mock estático nunca repinta — e em produção só repintaria depois do `aplicarTenantSalvo`. O sucesso era afirmado por dedução do contexto, não pela resposta do servidor. | Ver `memory/patterns.md` → "O que o servidor confirmou tem precedência sobre o contexto". O teste com contexto congelado é o detector natural desse defeito: se o feedback depende de um repintar assíncrono, ele falha lá e passa no navegador por sorte de temporização. |
| 2026-08-02 | `resumirAddonsDoTenant` (`src/lib/console.js`) cruzava catálogo e contratados com `linhas.filter((l) => l.tenant_id === tenantId)`. O `AddonsModal` calcula `tenant?.id ?? null` — então, sem tenant, `null === null` casava linha órfã e a tela mostrava "Ligado" para um add-on pago de ninguém. Ler o código não denuncia; só o teste pegou. | Filtro por `tenant_id` curto-circuita quando o tenant é ausente: `tenantId ? linhas.filter(...) : []`. Comparação por identidade com chave anulável **pareia duas ausências** — em SaaS multi-tenant é a forma mais barata de mostrar o dado de um cliente na tela de outro. Ver `memory/patterns.md` → "Cruzamento por tenant nunca casa tenant ausente". |
| 2026-08-02 | O bloco de falha de leitura do `AddonsModal.jsx` usava `.adm-erro`, `.adm-erro__titulo`, `.adm-erro__texto` e `.adm-relere` — nenhuma das quatro existia no `AddonsModal.css`. Nada acusa: o Vite não valida `className`, o jsdom não carrega CSS e a suíte fica verde. O estado de erro mais importante da tela sairia sem caixa, sem cor e sem alvo de toque. | Consequência direta da decisão 018 (CSS fora do JSX): classe inventada é falha **silenciosa**. Ao fechar tela nova, varrer os `className` do JSX contra o `.css` co-localizado — é o único jeito de pegar, e custa um `grep`. |
| 2026-08-02 | O `supabase/schema.sql` — o arquivo que o `CLAUDE.md` chama de "schema do banco em produção" — descrevia 26 tabelas quando as migrations já criavam 54. Faltavam **28 tabelas** e a camada multi-tenant **inteira**: nenhuma ocorrência de `tenant_id`, `tenants` ou `tenant_atual_id()` no arquivo que um dev novo lê para entender o banco. Ficou assim de 2026-07-04 a 2026-08-02, quatro semanas, com dezenas de migrations passando por cima. Ninguém percebeu porque o código roda contra o banco de verdade: só o documento mentia, e documento não quebra em runtime. | Documento que só uma pessoa lê envelhece sem sintoma — o sintoma tem que ser fabricado. `src/lib/schemaSqlGuard.test.js` compara o texto das migrations com o do schema e quebra a suíte no dia em que a divergência nasce, não quatro semanas depois. Regra geral: se o `CLAUDE.md` chama um arquivo de fonte de verdade, ele precisa de um teste que prove que ainda é. |
| 2026-08-02 | O mesmo guard quase nasceu com um furo silencioso: ele monta uma lista de tabelas derrubadas por `DROP TABLE` para **isentar** essas tabelas dos outros três testes — e a `20260903` derruba `_numeros`, uma `CREATE TEMP TABLE` de bloco de verificação. Uma temp table que um dia colidisse de nome com uma tabela real a tiraria da cobertura, calada. | Em guard textual, **conjunto de isenção é mais perigoso que conjunto de exigência**: errar para mais numa exigência gera falso positivo barulhento (alguém conserta), errar para mais numa isenção gera falso negativo mudo (ninguém sabe). Toda lista que serve para pular verificação precisa ser a mais estreita possível — aqui, `CREATE TEMP TABLE` é reconhecida e o `DROP` dela não entra. Vale para os outros treze `*Guard.test.js`. |
| 2026-08-02 | `registrarMovimentoCaixa` (`src/context/AppContext.jsx`) validava valor, motivo e numerário disponível, mas **não a sessão de caixa**. O botão da `Sidebar` só aparece dentro do ramo `caixaAberto`, e isso deu a impressão de que a regra estava aplicada — o edge case do spec dizia "o botão não existe **e o handler recusa**" e só a primeira metade existia. Com o modal já na tela e o caixa fechado em outro aparelho (`caixa_aberto` é chave do `config`, compartilhada e recebida por realtime), o insert passaria com o `sessao_aberta_em` de uma sessão já conferida: dinheiro fora de qualquer fechamento, sem erro em lugar nenhum. | Botão que some é conveniência, não regra. Toda condição de **estado compartilhado** é reconferida no handler que escreve, imediatamente antes do insert — a tela decidiu com o estado de segundos atrás. Ver `memory/patterns.md` → "Estado compartilhado é reconferido no handler, não só na tela". |
| 2026-08-02 | O `schemaSqlGuard` escrito na rodada anterior (TD016) quebrou a suíte **nesta** rodada, na primeira migration criada depois dele: `caixa_movimentos` existia em `supabase/migrations/` e não em `supabase/schema.sql`. Custou dois `Edit`; a divergência anterior do mesmo arquivo levou quatro semanas para ser notada e somava 28 tabelas. | O intervalo entre escrever o vigia e ele cobrar foi de **uma rodada**. Quando um arquivo do repositório precisa acompanhar outro e nada quebra quando divergem, o teste nasce junto do primeiro par — não depois do estrago. |

| 2026-08-02 | Migrar o hover do `.pdv__saldo-btn` para CSS revelou que ele montava `varColor(C.accent) + "66"` — a string `var(--gm-accent)66`, que é **CSS inválido**. O padrão funcionava quando as cores eram hex literal (`#7c3aed` + `66`) e morreu calado na migração para custom properties (ADR-007). Varrendo o `src/`: **18 ocorrências em 6 arquivos**. Não é só cosmético — como `var()` inválido resolve para `unset`, `border-style` cai para `none` e a borda **some** em vez de mudar de cor; no campo de senha da `Sidebar` (340, 359) é a borda vermelha de senha errada que desaparece. | Alfa sobre token só via `alfa(C.x, "NN")` (`src/constants/colorAlfa.js`) ou `color-mix` — **nunca** concatenando dígitos hex numa `var()`. Registrado como `BUG001` em `docs/09_BACKLOG/bugs.md` com a lista de linhas. Aprendizado maior: a migração de hex para custom property inverteu silenciosamente a validade de um idioma que estava espalhado pelo código, e nada no ferramental (Vite, jsdom, suíte) reclama de string de estilo inválida. Trocar a *forma* de escrever cor pede varredura pelo idioma antigo, não só pelos valores. |
| 2026-08-02 | A leva 1 do F018 no `PDVView` moveu `font-size`/`line-height` para classes com os tokens `--fs-*`/`--lh-*`, e parecia completa. Não estava: em `.pdv__lock-desc` o `lineHeight: 1.6` **inline** continuava vencendo a classe por especificidade, então o `var(--lh-base)` (1.5) escrito no CSS nunca chegou à tela. A classe existia, era lida como feita, e era letra morta. | Classe só entra em vigor onde nenhum inline declara a mesma propriedade — mover uma família de propriedades por vez (tipografia, depois cor) deixa classes que **parecem** aplicadas por uma leva inteira. Ao fechar uma leva, conferir se o inline que sobrou não declara o que a classe acabou de ganhar; e quando a leva seguinte remove o inline, o valor que aparece é o da classe, não o que estava na tela. Aqui a escolha foi preservar o 1.6 (contrato da rodada é mudança visual zero) e deixar o alinhamento com a escala como decisão de design. |
| 2026-08-02 | O inventário do `BUG001` fechado na rodada 13 (18 ocorrências em 6 arquivos) estava curto: o real é **26 linhas / 27 declarações em 7 arquivos**. O grep ancorava em `varColor(`, mas a forma quebrada também aparece quando o token chega por variável — `` `${tipo.color}44` `` em `RelatorioView`, `` `${cor}22` `` no `JarvasPanel`, `` `${color}18` `` no `ConfiguracoesView`, todas alimentadas por mapas que guardam `varColor(C.x)` — e quando o `}` fecha um ternário em vez da chamada (`` `${a ? varColor(C.green) : varColor(C.red)}44` ``). | Ao inventariar uma **forma** de defeito, faça o grep pela **cauda** (`+ "NN"`, `` `}NN` ``), que é o que não muda, e não pela cabeça, que varia. Depois confira a origem de cada variável antes de decidir que ela está fora do escopo. |
| 2026-08-02 | O mesmo defeito falha de dois jeitos, e procurar só um deles esconde metade. Em `border: \`1.5px solid ${...}\`` (atalho), o valor inválido em tempo de valor computado leva **todas** as longhands a `unset` → `border-style: none` → a borda some. Em `e.currentTarget.style.borderColor = ...` (CSSOM, nos `onFocus`/`onBlur` que várias telas fazem à mão), só a cor vai a `unset` → `currentColor` → a borda fica da **cor do texto**. | Quando o sintoma vier de `var()` inválido, verificar atalho e longhand separadamente: "a borda sumiu" não descreve o caso do `borderColor`, e um relato assim faz o segundo grupo passar batido. |
| 2026-08-02 | Ao extrair o CSS do modal "Saldo do Dia" descobri que o campo de senha (`PDVView/index.jsx`, `SaldoModal`) era **o único input do sistema que não mostrava foco**: a borda inline (`border: 1.5px solid ${senhaErro ? red : …}`) vencia por especificidade a regra do `src/styles/inputs.css` que pinta a borda de accent no `:focus`. Ninguém tinha reportado — o anel de `box-shadow` aparecia, então o campo parecia responder. O defeito nasceu junto com o inline e só ficou visível quando o inline saiu. | Estilo inline não só duplica o design system: ele **desliga** a parte dele que depende de estado (`:focus`, `:hover`, `:disabled`, `[aria-invalid]`). A regressão é sempre por omissão, nunca por erro visível na tela. Ao inventariar uma tela para o F018, o ganho não é só menos linha — é achar onde o baseline estava sendo silenciado. Ver `memory/patterns.md` → "Borda de input na classe entrega o foco para o inputs.css global". |
| 2026-08-02 | Nos cinco modais do `PDVView` (rodada 16), **três dos seis botões** tinham `disabled` com expressão diferente da que pintava o fundo: `!nome.trim() \|\| criando` contra `nome.trim()` ("Abrir"), `!pode \|\| transferindo` contra `pode` ("Transferir"), `salvandoMesa \|\| !mesaInput.trim()` contra `mesaInput.trim()` ("Entrar na comanda"). Trocar o ternário por `:disabled` — a leitura natural na hora de extrair o CSS — apagaria o botão **durante** a ação, enquanto o texto diz "Abrindo..."/"Transferindo...". Nenhum teste pega: a suíte não observa o meio da operação. E no "Confirmar Cancelamento" a divergência era por declaração, não por botão: o `background` casava com o `disabled`, o `box-shadow` dependia só de `motivo.trim()`. | `:disabled` só substitui o ternário quando as duas expressões são iguais **caractere a caractere**, e a comparação é por declaração, não por elemento. Onde diferem, o estado vira modificador de classe com a expressão de hoje (`--ativo`, `--pode`, `--salvando`) e o `:disabled` fica com o que de fato acompanha o atributo (`cursor`). Padrão em `memory/patterns.md` → "`:disabled` só substitui o ternário quando a expressão é a mesma"; tabela dos seis botões em `specs/f018-pdv-modais-css.md` §6. |
| 2026-08-02 | `.pdv__modal-erro` tinha **cinco** usuários, não quatro. O quinto era a linha "Apelido é opcional. Pressione Enter ou clique em Entrar para continuar." do modal de Mesa, em `muted` — texto informativo usando a classe só pelo tamanho da fonte. Dar à classe compartilhada o `color: var(--gm-red); font-weight: 600` que os outros quatro pediam pintaria de vermelho uma dica neutra, no modal que abre em toda comanda com mesa. O divergente não estava na tela que motivou a regra, que é exatamente por que ele passa batido. | Classe compartilhada nascida de extração de **tipografia** agrupa por tamanho, não por papel; o papel só aparece quando ela ganha cor. Antes de enriquecer, enumerar todos os usuários e conferir o valor de cada um — o que diverge sai para classe própria com a mesma tipografia (aqui `.pdv__mesa-hint`), e a compartilhada fica só com o que é igual em todos. Padrão em `memory/patterns.md` → "Enriquecer classe compartilhada: enumerar os usuários antes, não depois". |
| 2026-08-02 | `src/styles/tema.css:50` declara `--gm-warn: #f59e0b` e o comentário ao lado diz, com todas as letras, que o token existe para substituir o âmbar cravado inline no palm/mobile. Onze meses de código depois, o mesmo hex aparecia **seis vezes** só na faixa de alerta de estoque do `PDVView/index.jsx` — borda, fundo, ícone, label, toggle e chip — e a faixa de validade estava metade migrada (texto em `varColor(C.red)`, borda em `#ef444444`). Medindo o `src/` inteiro: **60 ocorrências de `#f59e0b` em 30 arquivos** (`grep -rn '#f59e0b' src --include=*.jsx --include=*.js --include=*.css`), quase metade delas já dentro de `.css` co-localizado — ou seja, arquivos que passaram pela decisão 018 e continuaram com a cor cravada. | Comentário de token descreve **intenção**, não faz varredura: enquanto o hex antigo não é grepado e listado, o token nasce parcial e o white-label (decisão 017) vale só nas telas que alguém lembrou de migrar. Token que substitui um valor literal nasce com o inventário do literal ao lado — e o inventário é do `src/` inteiro, `.css` incluído, porque extrair CSS do JSX não troca cor por token sozinho. |
| 2026-08-02 | Os dois grupos de abas do `PDVView/index.jsx` têm marcação equivalente, mas só o de cima (Mapa/Lista/Comandas/Reservas) declarava `fontFamily: "inherit"` inline; o do celular (Produtos/Carrinho) não. `<button>` não herda fonte do documento, então escrever `font-family: inherit` nas duas classes — a leitura natural de quem está normalizando CSS de botão — trocaria a fonte do sistema pela do app justamente no par de botões mais usado no celular. Só apareceu porque as duas extrações caíram na mesma rodada e a diferença ficou lado a lado. | Ao mover declaração de inline para classe, o conjunto de destino é **exatamente** o de origem: a ausência é copiada junto, mesmo parecendo descuido. Se a ausência for defeito de verdade, vira item separado — corrigir e migrar na mesma rodada apaga a evidência de qual das duas mudanças mexeu na tela, e é o que permite auditar 190 linhas trocadas por diferença lógica zero. Ver `memory/patterns.md` → "Ao extrair CSS, a classe copia a ausência também". |
| 2026-08-02 | Escrevi o comentário que explica o `--cor-status` como bloco JSX (`{/* … */}`) entre o `return (` e o `<div>` do callback de `.map()`, em `DeliveryView.jsx:628`. `{/* … */}` só é comentário **entre filhos** de um elemento; ali ele é uma segunda expressão dentro do parêntese, e o arquivo parou de compilar (`[PARSE_ERROR] Expected ',' or ')' but found 'Identifier'`, plugin `vite:oxc`). O disfarce foi o formato do relatório: `Test Files 1 failed \| 193 passed`, **`Tests 3071 passed` e nenhum teste falhando** — um arquivo que não transforma é contado como arquivo vermelho sem nenhuma asserção vermelha, o que se parece com flake. Pior: o diff normalizado que eu tinha acabado de rodar (regex que tira `style=`/`className=` e compara o resto) disse "só o comentário difere" e passou segurança sobre código que nem parseava. | Comentário que explica um `return` de JSX vai **acima** do `return`, com `//`. E, no vitest, arquivo vermelho com zero teste vermelho é erro de transformação, não instabilidade: ler a linha `[PARSE_ERROR]` antes de rodar de novo. O diff normalizado prova **equivalência de marcação** e nada mais — validade sintática só vem da suíte e do `vite build`. |
| 2026-08-02 | `.delivery-view__aviso--erro` era um modificador de **cor** que também carregava `margin-bottom: 12px`, e estava certo assim enquanto teve um usuário só (o aviso do topo da tela, `DeliveryView.jsx:399`). A aba Cardápio trouxe mais dois avisos de erro, os dois **dentro de modal** e sem margem nenhuma. Reaproveitar o modificador como estava — que é o que a leitura natural manda, a classe já existe e tem a cor certa — daria 12px de folga a dois avisos que nunca tiveram, dentro de um modal onde o espaçamento é apertado de propósito. Nenhum teste vê isso: a suíte não lê CSS. | Modificador que mistura **cor com espaçamento** só sobrevive ao primeiro usuário. Ao ganhar o segundo, o espaçamento sai para modificador próprio (aqui `--espacado`) e o original fica só com a cor — o novo usuário escolhe se quer a margem, em vez de herdá-la. Vale antes de reusar, não depois: a checagem é ler as declarações do modificador e perguntar quais delas são do **papel** (cor, peso) e quais são de **posição** (margem, largura). Padrão em `memory/patterns.md` → "Modificador de cor não carrega espaçamento". |
| 2026-08-02 | O helper `inputStyle(sz)` do `DeliveryView.jsx` não estava só duplicando CSS: estava **desligando** regra global. Ele devolvia `border` inline, e `src/styles/inputs.css` acende a borda no foco com `input:not([aria-invalid="true"]):focus { border-color: var(--gm-accent) }` — que perde para qualquer `border` inline. Resultado: os **20 campos** do Delivery eram os únicos do sistema que não acendiam ao focar, e ninguém tinha reportado porque a ausência de um realce não parece defeito, parece o normal. O cabeçalho do próprio `inputs.css` já previa isso ("Estilos INLINE no JSX ainda vencem por especificidade no que definem… Esses poucos são migrados nos próprios componentes"), ou seja, a regra global nasceu prometendo um comportamento que só chegou agora. | Ao contar o custo de um estilo inline, contar também **o que ele bloqueia**, não só o que ele repete: `border`, `background` e `color` inline atropelam as regras de **estado** (`:focus`, `:disabled`, `[aria-invalid]`) do baseline global, que é onde mora a consistência do sistema. Migrar inline para classe não é neutro — é entregar estados que estavam silenciosamente desligados. Isso é diferença visual legítima e vai declarada no spec, não descoberta na review. |
| 2026-08-02 | Dois dos treze objetos inline da fatia eram redundância pura: `style={{ ...inputStyle(sz), width: "100%" }}` nas linhas 1631 e 2667, sendo que `.delivery-view__input` já declarava `width: 100%` desde antes. Transformar cada propriedade extra em modificador — a leitura mecânica da tarefa — teria criado um `--largura-total` que não muda um pixel, e ele ficaria no CSS para sempre parecendo intencional. | Antes de virar modificador, ler a **regra de destino inteira**: parte do que o inline declara já pode estar na classe. A conferência é barata (um `sed` na regra) e a alternativa é CSS morto que a próxima pessoa vai tentar entender. |
| 2026-08-02 | O token novo ia nascer como `clamp(12px, 14.9px + 0.86vw, 48px)` — o `12px` copiado do degrau do celular mini, onde o `getSizes` de fato caía para 12. Escrito assim, o primeiro argumento é **inalcançável**: para o piso vencer seria preciso `14.9 + 0.86vw < 12`, ou seja, viewport negativo. O 12px real é entregue pela `@media (max-width: 359.98px)` logo abaixo, e o `clamp` ficaria com um número que nunca entra em jogo, no arquivo de tema, parecendo intencional para sempre. Peguei na conferência do próprio build, aplicando dentro da mesma fatia o aprendizado registrado na rodada anterior (o `--largura-total` que não mudava pixel). | **Função CSS de faixa (`clamp`, `min`, `max`) se confere argumento por argumento antes de commitar**: resolver a desigualdade e ver se cada um pode vencer em alguma largura real. Se o piso mora numa media query, a função é `min()` de dois argumentos — e o comentário diz onde o piso está. Copiar o valor do degrau para dentro do `clamp` "por simetria" é o caminho natural para o número morto. |
| 2026-08-02 | `src/components/desktop/views/CozinhaView.css:109` tinha `margin: 0 var(--gm-pad, 16px)` — referência a um token que **nunca foi definido em lugar nenhum**. Ninguém notou porque o fallback funciona: a faixa de erro da Cozinha desenhava 16px e parecia certa. Achei por acaso, ao rodar `grep` do nome antes de criar o token, e isso mudou a fatia: criar `--gm-pad` faz aquela linha **começar a resolver**, trocando 16px fixo por 12–48px responsivo, numa tela fora do escopo do spec. | `var(--token, fallback)` é indistinguível, na tela, de token existente — o fallback esconde a ausência em vez de denunciá-la. Antes de criar qualquer `--gm-*`, rodar `grep -rn 'var(--nome' src` para achar quem já referencia: essas linhas mudam de comportamento no instante da criação. Efeito colateral fora do escopo é aceitável quando é o que o autor da linha queria, mas vai **declarado no spec**, não descoberto depois. |
| 2026-08-02 | No Console, a lista de estabelecimentos passou a ser ordenada por urgência a partir de `resumirPlataforma`. Quando `listarAssinaturas` falha, o componente segue com `assinaturas = []` — e aí **todo mundo vira `sem_assinatura`**, o estado mais urgente de todos: a lista inteira subiria para o topo e a tela anunciaria "N estabelecimentos precisam de atenção" com N igual à base inteira, por causa de um erro de rede. O card já tratava isso ("Situação indisponível"), a ordem não trataria. | Derivado de leitura que pode falhar precisa checar a flag de erro **em cada consumidor novo**, não só no primeiro: em `ConsolePage.jsx`, `erroAssinaturas` agora também desliga a reordenação e a legenda. Lista vazia por erro e lista vazia legítima entram na mesma função pura como o mesmo argumento — quem sabe a diferença é só o chamador. |
| 2026-08-02 | A busca por nome do Console filtra a lista, e a legenda "N estabelecimentos precisam de atenção e aparecem primeiro" vinha do `precisamAtencao` do `resumirPlataforma` — ou seja, da base **inteira**. Buscando "cafe" com dois pendentes na base e só um no resultado, a tela mandaria o dono procurar um card que o próprio filtro tinha escondido. O bug não é do filtro nem do contador isoladamente: nasce de os dois lerem fontes diferentes. | Texto que fala sobre "a lista" tem que derivar **da lista renderizada**, não da lista de origem: em `ConsolePage.jsx`, `quantosPrecisamAtencao` conta `tenantsVisiveis` cruzando com um `Set` de ids pendentes. Ao introduzir filtro/paginação numa tela, varrer todo contador, resumo e legenda existentes e repontá-los para a coleção que o usuário vê. |
| 2026-08-02 | Escrever a classe de acentos `[̀-ͯ]` (do `normalize("NFD")`) por `node -e` gravou os **caracteres combinantes crus** no arquivo em vez das escapes — confirmado por `grep \| cat -v` (`[M-LM-^@-M-MM-/]`). Três tentativas de substituição repetiram o mesmo resultado, porque a própria string do script já vinha interpretada. Funciona igual em runtime, mas deixa bytes invisíveis no fonte, que qualquer editor ou merge pode destruir sem aviso. | Escape unicode em regex vai por **Edit direto no arquivo**, não por script de shell. Se precisar mesmo do script, montar a contrabarra pelo code point (`String.fromCharCode(92)`) e conferir com `cat -v` antes de dar por feito — a leitura normal do arquivo não distingue os dois casos. |
| 2026-08-02 | O botão "Registrar pagamento" no card do Console nasceu reusando `idsPrecisamAtencao` — o mesmo `Set` que ordena a lista. Reusar a régua inteira teria posto o botão em quem está `sem_assinatura`, que é justamente o **primeiro** da ordem de urgência: o clique abriria o modal e a RPC `confirmar_renovacao_assinatura` devolveria "Assinatura não encontrada", porque não há linha para renovar. O caminho desse tenant é definir a mensalidade na aba de cobrança, não pagar. | Régua de **urgência** e régua de **ação possível** não são a mesma coisa, mesmo quando a ação nasce da urgência: o estado mais grave costuma ser o que ainda não tem o que a ação escreve. Ao pendurar um botão num critério existente, listar estado por estado o que a escrita exige (`ConsolePage.jsx`: `idsPrecisamAtencao.has(id) && status !== "sem_assinatura"`, com `cancelado` já fora do `Set`) em vez de herdar o conjunto inteiro. |
| 2026-08-02 | Ao pendurar o `HistoricoPagamentosModal` no card do Console, o caminho natural era copiar os callbacks do uso que já existia no `PlanosDashboard.jsx` — lá `onEstornado` faz `setAviso(null)` além de recarregar, porque a tela dele tem uma faixa própria que ficaria mentindo. Copiado para a `ConsolePage`, esse `setAviso(null)` não teria contrapartida nenhuma. O oposto também apareceu: o `ConfirmarRenovacaoModal` **fecha** ao confirmar (`onConfirmado` limpa a linha) e o histórico **continua aberto** ao estornar — dois modais do mesmo domínio, no mesmo card, com ciclos de vida opostos, e cada um é o certo (renovar termina a tarefa; conferir pagamentos não). | Reusar componente num segundo lugar é redecidir os callbacks, não copiá-los: cada `on*` existe para um estado do **chamador anterior**, que pode não existir no novo. A conferência é olhar o que cada handler mexe e perguntar "isto existe aqui?" — em `ConsolePage.jsx` sobrou `onEstornado={() => carregar()}`, sem o `setAviso`. |
| 2026-08-02 | Os atalhos "Todos / Precisam de atenção / Em dia" do Console (`ConsolePage.jsx`) quase nasceram contando sobre `tenantsVisiveis` — a lista já recortada pela busca —, que é exatamente o que a legenda de urgência ao lado faz e está certa em fazer. Seriam dois números de aparência igual, um dizendo "quantos existem" e outro "quantos estão na tela", e o do botão mentiria: com "bar" digitado, "Precisam de atenção 1" abriria uma lista de cinco assim que a busca fosse limpa. | Número **dentro** de um controle promete o que clicar nele vai mostrar — conta sobre a base do controle, nunca sobre o resultado dos outros filtros. Número **fora** do controle descreve a tela e conta sobre o que está nela. Em `ConsolePage.jsx` são `contagens` (base `tenantsOrdenados`) e `quantosPrecisamAtencao` (base `tenantsVisiveis`), deliberadamente diferentes. |
| 2026-08-02 | O script node que devia acrescentar `filtrarPorSituacao` ao `import` da `ConsolePage.jsx` casou `"  filtrarEstabelecimentos,\n"` e **não substituiu nada, sem erro**: os fontes deste repositório são CRLF. O arquivo ficou referenciando função inexistente, e `src/lib/console.test.js` seguiu verde (a lib estava certa) — só o teste de tela pegaria. Mesma armadilha do guard textual (linha de 2026-08-01), agora na direção da escrita. | Edição pontual em fonte deste repositório vai de `Edit`, não de script com `replace`. Quando o script for inevitável, monte agulha e substituto com `\r\n` **e** falhe alto quando não casar (`if (novo === antigo) throw`) — substituição silenciosa é o modo de falha, não a exceção. |
| 2026-08-02 | Os testes do filtro na URL do Console nasceram assertando `window.location.search` — e passaram e falharam pelos motivos errados. `renderWithProviders` monta um `MemoryRouter`, que guarda o endereço em memória e **nunca toca em `window.location`**: as asserções positivas falharam com `expected '' to contain 'situacao=atencao'`, mas a negativa (`not.toContain("bar")`, provando que a busca fica fora da URL) passou vazia — teria passado mesmo se a busca fosse parar na URL. | Com `MemoryRouter`, o endereço só se lê perguntando ao roteador: renderize um espião com `useLocation()` ao lado do componente (`ConsolePage.test.jsx`, `EspiaoURL`) e asserte o texto dele. E asserção negativa sobre um valor que pode estar vazio por acidente não prova nada — prefira comparar o endereço inteiro (`toBe("/console")`) a dizer o que ele não contém. |

## Aprendizados de Produto

| Data | Aprendizado | Lição / Ação |
|------|-------------|--------------|
| 2026-06 | A "página em branco" é o maior inimigo da documentação: estrutura vazia desmotiva preenchimento. | Entregar esqueletos opinativos (princípio de produto nº 1). |
| 2026-08-02 | O Console passou a ligar NF-e e TEF por estabelecimento antes de qualquer dos dois funcionar de ponta a ponta: a emissão depende do certificado A1 e do CSC no servidor (decisão 026) e nenhuma maquininha está integrada — o PDV só simula a aprovação do cartão. Ligada sem aviso, a chave faria o dono vender ao cliente um recurso que ainda não existe. | Chave que liga recurso incompleto avisa **antes do clique**, na própria linha, o que falta para valer de verdade (`AVISOS_ADDON` em `src/constants/addons.js`). O texto é conhecimento da plataforma sobre a própria implementação: sai do código quando o add-on ficar pronto, e por isso nunca vira frase de cliente. |

## Aprendizados de Processo

| Data | Aprendizado | Lição / Ação |
|------|-------------|--------------|
| 2026-06 | Documentar a arquitetura **antes** de codar reduz retrabalho e alinha o time. | Manter abordagem document-first nas sprints de fundação. |
| 2026-06 | Conflitos entre instruções e a estrutura real do repositório devem ser resolvidos explicitamente, não assumidos. | Quando houver divergência entre o pedido e o que existe, registrar a decisão de mapeamento. |
| 2026-08-01 | A fila do dono dizia "preview clicável do cardápio — ✅ ENTREGUE (commit f9fc34f)" e o ledger da Rodada 3 recomendou o item dizendo que "não existe jeito de ver o cardápio como o cliente vê". As duas frases estavam erradas do mesmo jeito: o botão existia **e** abria o estabelecimento errado (a loja do fallback, para o dono de qualquer outro tenant). Marcar como entregue escondeu um furo de white-label por seis dias. | Antes de construir um item da fila, abrir o que já existe e ver **o que ele faz**, não se ele existe. "Entregue" e "não existe" são as duas respostas que dispensam o levantamento — e é exatamente aí que o defeito se esconde. Registro de entrega descreve o comportamento, não só o commit. |
| 2026-08-02 | A auditoria do backlog inteiro (TD016) encontrou **13 itens** cujo status estava errado, todos na mesma direção: `features.md` dizia "Backlog" ou "Planejado, código não iniciado" para F007–F013 e F016, que estão em produção há semanas. Uma rodada que confiasse nesse arquivo teria reconstruído KDS, estoque, financeiro, fiado, relatórios, Jarvas, gating de plano e enforcement de assinatura. O achado mais caro foi o inverso: **F021 (PDV offline-first) dizia "exige ADR antes de codar" e o código entrou sem ADR nenhum** — fila, replay da cascata e PWA rodando em `src/lib/offline/` + `AppContext`. | O backlog só é confiável se alguém conferir; conferir custa uma passada e evita rodada inteira jogada fora. Duas regras que saíram daqui: (1) status de item afirma ausência → conferir contra o código antes de usar como insumo; (2) item que exige ADR e é implementado sem ele deixa de ser dívida de código e vira dívida de decisão — registrar como pendência do dono, não como "entregue". |
| 2026-08-02 | Corrigir o backlog quase criou o problema que ele resolve: código entregue não é código rodando. As migrations `20260912`–`20260915` (analytics, estorno, identidade e add-ons do Console) estão no repositório e **não foram aplicadas em produção** — marcar os itens como "✅ Entregue" e parar aí trocaria uma mentira por outra, com a tela respondendo `function ... does not exist` para quem confiasse na linha. | Nota de entrega distingue as duas coisas explicitamente quando há migration pendente: "código entregue ≠ rodando em produção", com o número da migration. O guard do schema tem o mesmo limite e ele está declarado no cabeçalho: compara **texto** com texto, não sabe o que o banco tem. |
| 2026-08-02 | Dois critérios do spec do F005 se contradiziam e nenhum dos dois estava obviamente errado: o nº 9 mandava conferir a senha com `verificarSenhaAdmin(senha)` e o nº 3 exigia gravar `autorizado_por`. `verificarSenhaAdmin` (`src/lib/adminAuth.js:49`) aceita a senha de **qualquer** admin ou gerente ativo do tenant e devolve só `{ ok }` — não diz quem foi. Cumprir os dois ao pé da letra gravaria como autorizador o nome escolhido num seletor que ninguém conferiu. O `/build` desviou para `verificarSenhaUsuario(username, senha)`, que passa `p_username` para a mesma RPC. | Critério que **nomeia uma função** e critério que **exige um dado** têm que ser lidos juntos na hora de especificar: é a assinatura da função que diz se o dado existe. Conferir a linha de `export` antes de citar o helper no spec — foi o mesmo tipo de erro do `logoUrlSegura` inexistente, uma rodada antes. |
| 2026-08-02 | A nota do F018 em `docs/09_BACKLOG/features.md` registrava "2068 `style={{` em 46 arquivos", mas medindo hoje o `HEAD` dá **2050**. Ninguém errou a conta: a medição de origem usou um recorte um pouco diferente (provavelmente incluindo `.jsx` de teste) e a nota guardou só o número, não o comando. Duas rodadas depois já não dá para saber se a diferença é progresso ou escopo. | Métrica de backlog que mede progresso guarda o **comando** ao lado do número, não só o número — aqui `grep -ro 'style={{' src --include=*.jsx \| wc -l`. Sem isso, a próxima medição não é comparável com a anterior e o item perde a única evidência objetiva de que está andando. |
| 2026-08-02 | O spec da rodada 19 fixou o alvo como "**49 → 8**" e as duas pontas contavam padrões diferentes: o 49 era só `grep 'style={{'`, e o 8 incluía também os `style={inputStyle(sz)}`, que têm uma chave só. A faixa tinha, de fato, **53** atributos `style`. O número não estava errado por descuido de conta — estava errado porque a frase não dizia o que contava, e na review eu bati 8 contra 49 e a subtração não fechou com a queda do `src/` (1807 → 1762, 45 e não 41). Custou uma rodada de checagem para descobrir que os dois números estavam certos e a comparação é que não existia. | Alvo verificável de spec guarda o **padrão exato** que a review vai rodar, idêntico nas duas pontas — "53 → 8 atributos `style` (49 `style={{` + 4 `style={inputStyle`)". É a mesma regra que já vale para métrica de backlog (guardar o comando ao lado do número), aplicada dentro do spec: sem o padrão escrito, "de X para Y" não é verificável, é só plausível. |
| 2026-08-02 | O normalizador de markup (`/tmp/norm.js`, que apaga `style=`/`className=` por contagem de chaves e compara o resto) foi escrito esperando saída "DIFF VAZIO", e nesta rodada ele disse **DIVERGE no offset 76127** — corretamente, porque a fatia apagou uma função inteira, que não é markup. Ler isso como falha é o reflexo errado; ler como sucesso sem conferir é pior. O que fecha a prova é outra coisa: o arquivo novo **termina** exatamente no offset da divergência (76127 de 76127) e o resto exclusivo do `HEAD` é literalmente o corpo de `inputStyle`. | Quando a fatia apaga código que não é marcação, o diff normalizado esperado **não é vazio** — é exatamente o trecho apagado, e a review precisa provar isso: comparar o comprimento do arquivo novo com o offset da divergência e imprimir o resto exclusivo do `HEAD`. "DIFF VAZIO" só é o critério certo quando a rodada não remove nada além de atributo. |
| 2026-08-02 | O normalizador de markup voltou a dizer **DIVERGE** — desta vez logo no primeiro import, porque a fatia apagava props (`sz={sz}`) e imports, que não são marcação. Ele só reporta o **primeiro** ponto de divergência, então parar ali não prova nada sobre o resto do arquivo: ficaria a palavra do `git diff` lido a olho contra 57 linhas alteradas. A saída foi inverter a pergunta: em vez de esperar diff vazio, declarei no script as **4 transformações que a fatia autoriza** (dois imports, o `const sz`, ` sz={sz}` e `{ sz, `), apliquei só no `HEAD` e exigi igualdade — que veio. | Quando a rodada muda mais que atributo de estilo, o normalizador vira **verificador de transformação declarada**: escreva a lista fechada de substituições permitidas, aplique no `HEAD` e exija DIFF VAZIO contra o arquivo novo. Assim "nada além do previsto mudou" volta a ser prova mecânica, e a lista de substituições é, de quebra, a descrição exata da fatia. Comparar comprimento e imprimir o resto exclusivo (rodada 20) serve para remoção de bloco; esta versão serve para reescrita espalhada. |

| 2026-08-02 | Migrar 91 `style={{}}` à mão é caro e um `replace all` cego é pior: três blocos idênticos aos da fatia moravam em `AbaEntrega` (`DeliveryView.jsx` 2660, 2684 e 2690), que é escopo da fatia 10. Um replace-all os teria migrado junto e nada no relatório apontaria isso. O script de migração declarou **quantas ocorrências cada substituição espera** e se recusou a gravar enquanto a conta não fechasse — foi ele que apontou os três gêmeos, um por um. | Script de migração em massa **conta antes de gravar**, acumula todos os erros numa execução só (em vez de lançar no primeiro) e não escreve nada se qualquer contagem divergir. Contagem que não bate se resolve **estreitando a âncora** (indentação, ou um pedaço do handler), nunca subindo o esperado. E a conferência final é uma linha de Node: extrair as classes usadas no JSX e as declaradas no CSS e listar a diferença — classe aplicada sem regra não é reclamada nem pelo `vitest` (não lê CSS) nem pelo `vite build` (CSS órfão compila). |
| 2026-08-02 | A fatia 10 apagou a **última** chamada de `alfa()` do `DeliveryView.jsx` e o import do helper ficou órfão no topo do arquivo. Nada acusou: o `vitest` passou (195/3096) e o `npx vite build` compilou verde — import não usado é código válido, e o bundler simplesmente não o inclui. Só apareceu porque eu rodei `grep -c "alfa(" ` depois da migração e o resultado foi **0**, não 1. | Script de migração em massa que troca chamada de helper por CSS termina conferindo se o helper **ainda tem usuário no arquivo**: contagem que cai a zero significa import morto, e nenhuma das duas suítes de gate (`vitest`, `vite build`) denuncia isso. A conferência é a mesma linha que já se roda para as classes órfãs — muda só o que se conta. |
| 2026-08-02 | Na fatia 11 eu escrevi o `Td` como `style={color ? { "--td-cor": color } : undefined}` para não pendurar custom property vazia. O inline continua lá, mas **some da métrica do F018**: o `grep 'style={{'` não casa com `style={color ?`. A contagem veio 158/2 em vez dos 159/3 que o spec previa — ou seja, *melhor* que o previsto, que é justamente a direção suspeita. Troquei por `style={{ "--td-cor": color }}`; o React ignora propriedade `undefined`, o comportamento é idêntico e o inline volta a ser contado. | Enquanto o F018 é medido por `grep 'style={{'`, qualquer forma que fuja desse literal (`style={cond ? {…} : undefined}`, `style={objetoDeVariável}`) **melhora o número sem tirar o inline**. Métrica fica honesta batendo o resultado contra o previsto no spec e desconfiando quando ele vem melhor: queda maior que a prevista é sinal de inline escondido, não de bônus. |
| 2026-08-02 | Ao reescrever o cabeçalho do `RelatorioView.css` citei os tokens como `--fs-*/--lh-*` dentro do bloco `/* … */`. O `*/` no meio da frase **fecha o comentário ali**, e as ~15 linhas seguintes de texto em português viram CSS inválido. Peguei relendo o que tinha escrito, antes de compilar — e o `vite build` provavelmente teria passado assim mesmo, só cuspindo mais um `css-syntax-error` no meio dos avisos pré-existentes que a gente já aprendeu a ignorar. | Em comentário de CSS, glob de token nunca leva `/` colado no `*`: escrever `--fs-` e `--lh-` separados, ou `--fs-* e --lh-*`. Vale como cuidado geral: **o repositório já normalizou avisos de `css-syntax-error` como ruído**, então erro real dentro de comentário passa despercebido no gate — a revisão do texto do comentário é manual.|
| 2026-08-02 | O bloco de conferência da `20260913_estorno_pagamento_assinatura.sql` reprovava a própria função que acabara de criar: o guarda "o estorno não recebe o estabelecimento por parâmetro" era `v_def LIKE '%p_tenant_id%'`, e em `LIKE` o `_` é curinga de um caractere — o padrão casava com o alias `p.tenant_id` que o corpo usa ao recalcular `ultima_renovacao`. A migration abortava sempre, no SQL Editor, acusando um defeito inexistente. Nenhum gate pegou: o `vitest` não roda SQL e a conferência só executa no banco. | Guarda de migration que procura um literal usa `position(txt in v_def) > 0`, nunca `LIKE`. Em `LIKE`, `_` e `%` são curingas e identificador de SQL é cheio de `_`; o risco é só no teste **positivo** (`LIKE` verdadeiro = falha), porque em `NOT LIKE` o curinga apenas afrouxa. Ao escrever a conferência, rodar o padrão contra o próprio corpo antes de confiar nele. |

| 2026-08-03 | A segunda linha de atalhos do Console (recorte por plano) quebrou **7 testes das rodadas 6 e 7** sem nenhum defeito no código novo: o helper `atalho(nome)` do `ConsolePage.test.jsx` procurava o botão em `screen` inteiro por prefixo (`new RegExp('^' + nome)`), e "Todos os planos" passou a casar com o `^Todos` do atalho de situação — `Found multiple elements`. A tentação era renomear o botão novo para o seletor voltar a ser único, ou seja, deixar o teste escolher o texto da tela. | Helper de teste que procura **botão de um grupo** se escopa no grupo desde o primeiro dia: `within(screen.getByRole("group", { name: /Filtrar por situação/i })).getByRole("button", …)`. Seletor global por prefixo é uma bomba-relógio em tela que ganha controles — e quando ele estoura, a correção é escopar o seletor, nunca mudar o rótulo que o usuário lê. |

## Aprendizados de Negócio

| Data | Aprendizado | Lição / Ação |
|------|-------------|--------------|
| 2026-06 | O valor central do GastroMundi (integração a partir da venda) precisa ser sentido cedo: o usuário tem que ver uma venda no PDV se propagar para caixa, estoque e relatórios. | Priorizar o "aha moment" da venda-fonte no MVP (PDV → Caixa → Pedidos). |

## Fluxo de criação do Console só é testável com a borda dublada (rodada 37)

`ConsolePage.test.jsx` dubla `@/lib/console` com `...real` e só troca as
leituras. Para chegar ao cartão de primeiro acesso pelo caminho de verdade
(preencher o formulário e enviar) foi preciso somar
`provisionarEstabelecimento` à lista dublada: ela é a única porta que cria
`auth.users` e chamá-la de verdade num teste não faz sentido. A validação do
formulário (`validarNovoEstabelecimento`) fica real de propósito — é ela que o
teste exercita ao digitar. Da próxima vez que um teste do Console precisar
passar pela criação, o mock já está pronto: `mockProvisionar`.

## Asserção de "não contém o valor" casou com a URL do teste (rodada 38)

Em `src/pages/console/ConsolePage.test.jsx`, provar que a mensagem copiada não leva valor
financeiro com `expect(texto).not.toMatch(/300/)` falhou: o endereço de entrada do jsdom é
`http://localhost:3000` e contém "300". Asserção negativa sobre dinheiro precisa mirar a forma
do dinheiro (`/R$/`, `/300,00/`, o rótulo "mensalidade"), não o número solto — senão o teste
quebra por coincidência de dígitos com porta, id ou data.

## Dois botões laterais no mesmo card não podem repetir o ícone (rodada 39)

Em `src/pages/console/ConsolePage.jsx`, "Registrar pagamento" e o novo "Definir mensalidade"
nasceram os dois com `LuBanknote`. As duas réguas se cruzam — quem está vencendo E sem preço
mostra os dois botões lado a lado, com o mesmo desenho de nota de dinheiro e ações diferentes
(uma empurra o vencimento, a outra grava o preço). O segundo virou `LuTag` (etiqueta de preço).
Regra para os próximos botões de card no Console: antes de escolher o ícone, verificar se a
condição que mostra o botão pode ser verdadeira junto com a de algum vizinho.

## Status de assinatura em teste vem da DATA, não do campo `status` (rodada 40)

Em `src/pages/console/ConsolePage.test.jsx`, forçar `status: "bloqueado"` na assinatura dublada
não bloqueou nada: `resumirPlataforma` **recalcula** o status por `calcularStatusAssinatura`
(só `cancelado`, que é manual, sobrevive ao recálculo). O teste de ordem por urgência só passou
depois de mudar o `data_vencimento` para uma data vencida fora da carência. Para fabricar
situação de cobrança em teste, mexa na data — o campo `status` do banco é cache.


## No host do Console, `?loja=` perde para o subdomínio (rodada 41)

O atalho "Ver cardápio" do Console abre a vitrine de OUTRO estabelecimento, e a precedência
de `slugDaVitrine` (`src/lib/tenantSlug.js`) é **subdomínio > query > fallback**. Num Console
que roda em `console.<dominio>` (o host dedicado do ADR-008 §7), o rótulo "console" é uma
reivindicação de subdomínio válida e vence o `?loja=`: o link abriria a loja errada, ou
nenhuma. Por isso `urlDoCardapioPublico` devolve o endereço publicado
(`https://<slug>.<root>/cardapio`) quando `ehConsoleHost()` é verdadeiro, e só usa o caminho
relativo com `?loja=` fora dele. Qualquer link novo do Console para uma superfície de tenant
tem que responder a mesma pergunta: de que host ele vai ser clicado?

## Nome acessível de botão do Console vem do texto, não do `title` (rodada 41)

Em `src/pages/console/ConsolePage.test.jsx`, `getByRole("button", { name: /Trocar o layout/i })`
não encontrou nada: os botões laterais do card usam o `title` como dica de mouse e trazem no
corpo um rótulo curto (o nome do layout, "Sem add-ons"), que é o que vira nome acessível. Para
mirar esses botões em teste, use `getByTitle` — ou o rótulo curto, quando ele for estável.

## Hoje a porta de entrada é a mesma para todo tenant — e isso tem prazo (rodada 42)

Ao escrever o "Copiar acesso" do Console foi preciso responder qual endereço mandar ao
cliente. O login monta o e-mail do Auth como `${username}@${slug}.local`, e o slug vem do
SUBDOMÍNIO. Sem `VITE_ROOT_DOMAIN`, `emailDoLogin` cai no fallback `gastromundi`, e a Edge
Function `provisionar-estabelecimento` usa o MESMO gate (`TENANT_ROOT_DOMAIN` desligado ⇒
namespace `gastromundi`, linha ~142). Ou seja: hoje a origem compartilhada realmente
autentica qualquer estabelecimento, desde que o username seja único — por isso
`urlDeAcessoDoTenant` devolve a origem do navegador nesse estado. No dia em que o domínio
raiz for ligado, o endereço de CADA cliente vira `https://<slug>.<root>` e qualquer texto
que ainda mande a origem estará errado (a origem do dono pode até ser o host do Console).
Quem tocar em endereço de acesso tem que olhar os dois gates, front e Edge Function.

## `toHaveValue` não aceita matcher assimétrico (rodada 42)

Em `src/pages/console/ConsolePage.test.jsx`, `expect(campo).toHaveValue(expect.stringContaining(...))`
falhou mesmo com o texto certo dentro do campo — a mensagem de erro mostra o valor
esperado e o recebido praticamente iguais, o que engana. `toHaveValue` compara valor
exato; para trecho, leia `campo.value` e use `toContain`.

## O Console LÊ `tenants`, não escreve — a RLS só tem SELECT (rodada 43)

Duas rodadas seguidas quase viraram "deixar o Console editar o estabelecimento"
(trocar o nome, corrigir o endereço/slug). Não dá sem mudança de schema: as
policies de `tenants` são só de leitura — `tenants_select_auth`, criada em
`20260716_tenants_minimo.sql` e redefinida em
`20260726_multitenant_fase4_billing_isolamento.sql`. Não existe policy de UPDATE
para o super-admin. Toda escrita em `tenants` hoje passa por Edge Function /
RPC `SECURITY DEFINER` (é assim que o provisionamento cria). Antes de
especificar edição de estabelecimento no Console, ou se cria a RPC (decisão do
dono, mexe em produção) ou o item não é dessa rodada. E trocar o slug ainda
quebraria links de cardápio já entregues ao cliente.

## `VITE_ROOT_DOMAIN` é lido na CARGA do módulo — `vi.stubEnv` não alcança (rodada 44)

`src/lib/tenantSlug.js` guarda `const ROOT_DOMAIN = import.meta.env.VITE_ROOT_DOMAIN` no
topo do arquivo. Depois do import, mudar a variável de ambiente no teste não muda mais
nada: `urlDeAcessoDoTenant` continua no ramo "sem domínio raiz" e nunca devolve `null`.
Para exercitar o estado "domínio raiz ligado" em teste de componente (o único em que a
origem do navegador e o endereço do cliente deixam de ser o mesmo lugar), mocke o módulo
repassando `rootDomain` nas opções — as duas funções já aceitam esse override testável:
`vi.mock("@/lib/tenantSlug", ...)` com `importActual` e um holder `vi.hoisted` que, vazio,
deixa tudo real. Feito assim em `src/pages/console/ConsolePage.test.jsx`. Reimportar a
página inteira com `resetModules` também funcionaria, mas o arquivo tem sete `vi.mock` e
sairia bem mais caro.

## O `maxLength={30}` do Console é amarração de teste, não número solto (rodada 47)

`src/lib/provisionamentoValidacao.test.js` lê o TEXTO de
`NovoEstabelecimentoModal.jsx` (`readFileSync` + regex sobre `value={campo}` …
`maxLength={\d+}`) e compara com `MAX_NOME`/`MAX_ADMIN_NOME`/`MAX_USERNAME`/
`MAX_SENHA` da borda. O objetivo é ter uma fonte independente: se o teto virasse
uma constante importada, o teste compararia a constante consigo mesma. Trocar
`maxLength={30}` por `maxLength={MAX_USERNAME}` na rodada 47 fez a regex devolver
`null` e derrubou o arquivo inteiro. Antes de "limpar magic number" nesses quatro
campos do modal, leia o teste: o literal está ali de propósito. Constante nova que
precise do mesmo teto ganha a própria linha de amarração no mesmo describe
(`MAX_USERNAME` do Console = `maxLength` do campo = `MAX_USERNAME` da borda).
