# Decisões do Projeto GastroMundi

## Objetivo
Registrar todas as decisões relevantes tomadas ao longo da vida do projeto, com contexto, justificativa e impacto. É o histórico permanente de "por que fizemos assim".

## Contexto
Decisões não documentadas se perdem com a rotatividade de equipe. Este arquivo complementa `docs/08_DECISOES/` (ADRs detalhados) com um resumo navegável de todas as decisões-chave.

## Regras Gerais
- Toda decisão arquitetural, de stack ou de produto deve ser registrada aqui
- Cada entrada deve conter: data, decisão tomada, alternativas consideradas e motivo da escolha
- Decisões reversas (mudanças de decisão anterior) devem referenciar a decisão original

## Validações
- Novas decisões não podem contradizer decisões ativas sem registro de revisão
- Decisões com impacto em segurança ou dados exigem aprovação de, no mínimo, dois membros sênior

## Permissões
- Qualquer membro da equipe pode propor uma decisão
- Decisões de arquitetura exigem aprovação do tech lead
- Decisões de produto exigem aprovação do product owner

## Exceções
- Decisões emergenciais (hotfix, incidente) podem ser tomadas por um único responsável, com registro retroativo obrigatório em até 48h

## Auditoria
- Cada entrada deve conter: autor, data, status (ativa | revisada | descontinuada)
- Revisões periódicas recomendadas: semestrais

## Eventos
- `decision.added` — nova decisão registrada
- `decision.revised` — decisão anterior substituída ou atualizada
- `decision.deprecated` — decisão marcada como descontinuada

## Configurações Futuras
- Automatizar linkagem entre este arquivo e os ADRs em `docs/08_DECISOES/`
- Criar template de proposta de decisão para uso em reuniões

## Casos de Uso
- Onboarding técnico de novos desenvolvedores
- Revisão de arquitetura
- Avaliação de refatorações ou migrações
- Auditoria pós-incidente

## Critérios de Aceite
- [ ] Formato padronizado para todas as entradas
- [ ] Referência cruzada com ADRs detalhados
- [ ] Status de cada decisão atualizado

---

## Registro de Decisões

> Resumo navegável das decisões-chave (arquiteturais iniciais). ADRs detalhados vivem em `docs/08_DECISOES/`.

| # | Data | Decisão | Alternativas | Motivo | Status | ADR |
|---|------|---------|--------------|--------|--------|-----|
| 001 | 2026-06-15 | Stack: React + Vite + Tailwind + Supabase | Next.js, Remix, Firebase, backend Express próprio | Velocidade no early stage, sem servidor próprio, PostgreSQL+RLS, ótima DX | Revisada (ver 011, 012) | [ADR-001](../docs/08_DECISOES/adr-001.md) |
| 002 | 2026-06-16 | Multi-tenancy por **tenant_id compartilhado** com isolamento via RLS | Schema por tenant; banco por tenant | Simplicidade operacional no MVP, isolamento garantido por RLS, custo controlado | Ativa | — |
| 003 | 2026-06-16 | Estado de servidor via camada de data-fetching com cache; estado global de UI via **Context API** | Redux, MobX | Context cobre as necessidades atuais sem boilerplate; evita over-engineering | Ativa | — |
| 004 | 2026-06-16 | **Event Bus** interno para comunicação desacoplada entre módulos | Acoplamento direto via chamadas | Reduz acoplamento, habilita auditoria e reações cross-módulo | Ativa | — |
| 005 | 2026-06-16 | **Feature Flags** desde o MVP | Deploy direto sem flags | Permite entregas graduais, testes A/B e kill-switch sem novo deploy | Ativa | — |
| 006 | 2026-06-16 | Validação de contratos com **Zod** em todas as fronteiras | Validação manual; sem validação | Segurança de tipos em runtime, contrato único front/back | Ativa | — |
| 007 | 2026-06-16 | Isolar acesso ao Supabase em **camada de serviços** | Chamadas diretas nos componentes | Reduz lock-in e facilita testes/migração futura | Revisada (ver 011) | — |
| 008 | 2026-06-16 | Papéis operacionais: **dono / gerente / caixa / atendente / cozinha**; planos: **free / pro / enterprise** | Papéis genéricos (member/admin/owner) | Refletem a operação real de restaurante-varejo; permissões por função | Revisada (ver 020 — nomes/granularidade de plano) | [ADR-005](../docs/08_DECISOES/adr-005.md) |
| 009 | 2026-06-16 | A **venda no PDV é a transação-fonte**: emite eventos consumidos por Caixa, Estoque, Financeiro, Relatórios e Jarvas | Atualização manual em cada módulo | Garante consistência automática entre módulos via Event Bus (decisão 004) | Ativa | — |
| 010 | 2026-06-16 | **Jarvas** é uma **camada transversal de IA** que observa eventos de todos os módulos | IA acoplada a um único módulo (chat) | Permite insights, detecção de padrões e sugestões cross-módulo | Ativa | — |
| 011 | 2026-06-16 | **Caminho A:** acesso a dados via **API Express (contract-first) + Drizzle**; Supabase passa a ser **apenas o PostgreSQL gerenciado** (sem SDK direto no frontend, sem Edge Functions por ora) | Caminho B (frontend → Supabase SDK + RLS + ponte Clerk→Supabase); Replit Postgres nativo | Aproveita o scaffold do monorepo (Express/Drizzle/Orval/Zod) e a tipagem ponta a ponta; centraliza regras na API; evita ponte JWT frágil. Revisa 001 e 007 | Revisada (ver 015 — roadmap) | [ADR-002](../docs/08_DECISOES/adr-002.md) |
| 012 | 2026-06-16 | **Autenticação via Clerk** (gerenciado pela Replit), cookie de sessão + middleware Express, no lugar do **Supabase Auth** | Supabase Auth; Replit Auth | Baixa fricção, gerenciado, sem necessidade de ponte JWT com Supabase. Revisa 001 | Revisada (ver 015 — roadmap) | [ADR-002](../docs/08_DECISOES/adr-002.md) |
| 013 | 2026-06-16 | Versões/ferramentas reais: **React 19 / Vite 7 / Tailwind 4 / wouter**; estado de servidor via **TanStack Query** (hooks gerados pelo Orval a partir do OpenAPI) | React Router; data-fetching a definir | Reflete o scaffold nativo do ambiente; corrige versões do ADR-001 | Revisada (ver 015 — app real usa React 18 / React Router v6) | [ADR-002](../docs/08_DECISOES/adr-002.md) |
| 014 | 2026-07-03 | **Rebrand: Kora → GastroMundi.** Produto, docs e memória renomeados globalmente; arquitetura e regras inalteradas; Jarvas mantém o nome por ora | Recomeçar do zero; manter dois nomes | Preservar a fundação documental sob a marca definitiva | Ativa | [ADR-003](../docs/08_DECISOES/adr-003.md) |
| 015 | 2026-07-03 | **Junção com o app em produção: stack real prevalece** — React 18 + Vite + Supabase direto (SDK, Auth, RLS) + React Router v6 + Vercel. ADR-002 (API Express + Drizzle + Clerk) rebaixado a roadmap; schema real em `supabase/` | Migrar o app para o monorepo; manter dois repositórios | Doc não pode contradizer a produção; migração grande e arriscada sem ganho imediato | Ativa | [ADR-004](../docs/08_DECISOES/adr-004.md) |
| 016 | 2026-07-05 | **Módulo Financeiro fase 1**: lançamentos (`public.lancamentos`) alimentados pela venda já normalizada (`vendas`/`venda_itens`/`venda_pagamentos`, TD009), não pelo blob `sales`; **fiado é modelado como conta a receber** (`status='previsto'`, vencimento em 30 dias) em vez de um lançamento à parte — mesma tabela e mesmo fluxo de baixa das demais contas | Lançamento de "fiado" com tipo próprio; alimentar o Financeiro a partir de `sales` (blob) | Reaproveita a normalização já feita (TD009) em vez de duplicar parsing de JSONB; tratar fiado como conta a receber comum simplifica a baixa e o fluxo de caixa (mesmo modelo previsto/realizado, sem status especial) | Ativa | — |
| 017 | 2026-07-06 | **Produto é SaaS multi-estabelecimento white-label, não um sistema de cliente único.** Hoje atende o estabelecimento GastroMundi, mas o objetivo é vender em larga escala para vários estabelecimentos. O sistema deve ser **adaptável por estabelecimento**: identidade visual (marca, logo, cores, nome), configurações e módulos ativos configuráveis por tenant. A marca "GastroMundi" é a plataforma/produto; cada cliente pode ter sua própria identidade sobreposta. Alinha-se ao multi-tenancy por RLS (decisão 002) e ao sistema de planos (F013). | Sistema single-tenant fixo; um fork/clone por cliente | Escala de vendas exige um único codebase adaptável; clonar por cliente inviabiliza manutenção; branding por tenant é requisito de venda white-label | Ativa | [ADR-005](../docs/08_DECISOES/adr-005.md) · [ADR-007](../docs/08_DECISOES/adr-007.md) |
| 027 | 2026-07-10 | **Dois níveis de administração distintos.** (a) **Console da plataforma (super-admin / dono do SaaS)** — login próprio, papel elevado CROSS-TENANT: cria estabelecimentos (onboarding), define plano/add-ons por tenant, concede/estende tempo de assinatura, configura o fiscal de cada cliente, vê billing de todos os tenants. Opera acima do isolamento por tenant. (b) **Configurações do estabelecimento (admin do tenant)** — o dono do restaurante, escopado só ao próprio tenant. SEGURANÇA: o super-admin é a superfície mais sensível (acesso cross-tenant total) — papel separado (ex.: `plataforma`/`super_admin`, não é um `admin` de tenant), e as policies de RLS por tenant precisam de um override explícito `OR auth.is_super_admin()`; desenhar junto do isolamento multi-tenant (S1-1), pois são dois lados da mesma moeda. | Um único nível de admin; super-admin como flag improvisada | Vender SaaS exige um back-office cross-tenant para operar contas/assinaturas, separado e mais protegido que o admin do cliente | Ativa | S1-1 / F022 |
| 026 | 2026-07-10 | **Credenciais fiscais são configuração POR TENANT, em branco por padrão, preenchidas no onboarding.** Certificado A1, senha do certificado, CSC/token (NFC-e), inscrição estadual, regime tributário, ambiente (homologação/produção) e conta/token do provedor fiscal são campos por estabelecimento — vazios até o dono fechar contrato e informar os dele. Nada hardcodado. SEGURANÇA: o certificado A1 e senhas são secretos — NUNCA vão ao bundle/frontend; ficam server-side (Supabase, acesso restrito por RLS/tenant) e a emissão passa por Edge Function (mesmo padrão de `jarvas-assistente`/chave da Anthropic), que fala com o provedor fiscal; o frontend nunca segura o certificado. | Certificado único hardcodado; segredos no frontend | White-label multi-tenant: cada CNPJ tem seu certificado; e segredo fiscal no cliente seria falha grave de segurança | Ativa | F019 / S1-3 (tela admin) / S1-4 |
| 025 | 2026-07-06 | **Impressão via abstração de "driver", com implementações trocáveis — QZ Tray NÃO é dependência cravada.** Os layouts (F015/F020) falam com uma interface de driver de impressão; QZ Tray (ESC/POS) e browser-raster (window.print) são implementações por trás dela, substituíveis (ex.: WebUSB/WebSerial, agente local, outro). Motivos: (a) evitar lock-in; (b) custo — QZ Tray exige certificado de assinatura pago para impressão silenciosa sem aviso em produção (regra de Custo → adiar o pago, manter o gratuito browser-raster como default). | Cravar QZ Tray como único caminho de impressão | Flexibilidade por estabelecimento/impressora e não amarrar o produto a um fornecedor pago na fase de bootstrap | Ativa | F020 / ADR-007 |
| 019 | 2026-07-06 | **Emissão de nota fiscal (no pagamento) e integração TEF são add-ons pagos transversais, não recursos de tier.** Estão disponíveis como **opção em todos os planos** (Básico → Avançado), com **valor adicional** por cima da mensalidade do plano. Diferem do gating por plano (decisão 017/F013): não são liberados por nível, e sim contratados à parte por qualquer cliente. **Devem ser nativos no código** — a arquitetura já nasce com os pontos de extensão (fiscal no fluxo de pagamento; TEF no PDV), habilitáveis por flag de add-on no tenant, não como integração improvisada depois. | Amarrar NF-e/TEF a um tier alto; construir como plugin externo/gambiarra futura | Add-ons pagos transversais maximizam receita (qualquer cliente pode contratar) e exigem que os hooks estejam no core desde já para não virar retrabalho | Ativa | [ADR-005](../docs/08_DECISOES/adr-005.md) · [ADR-006](../docs/08_DECISOES/adr-006.md) |
| 018 | 2026-07-06 | **Separar CSS do JSX** (não misturar estilo e marcação no mesmo arquivo) para facilitar edição de layout a longo prazo e a customização visual por estabelecimento (decisão 017). Padrão a definir no ADR (CSS Modules, arquivos `.css` co-localizados, ou tokens/tema por tenant), mantendo compatibilidade com o Tailwind já em uso. | Manter estilos inline/classes Tailwind espalhadas no JSX | Layouts editáveis e white-label por tenant ficam inviáveis com estilo acoplado à marcação; separação melhora manutenção e temas | Ativa | [ADR-007](../docs/08_DECISOES/adr-007.md) |
| 020 | 2026-07-06 | **Planos revisados para 5 tiers superconjunto** — Básico (Cardápio/Produtos+PDV+Caixa) → Simples (+Estoque+Pedidos) → Médio (+Mesas/Comandas+Cozinha+Alertas) → Alto (+Financeiro+Clientes/Fiado+Relatórios) → Avançado (+Jarvas+multi-loja+fiscal/integrações nativas). Registro central em tabelas `planos`/`planos_modulos` (Postgres), não `if/else` no código. Gating em 3 camadas: Sidebar/rotas (esconde), RLS/RPC (bloqueia de verdade), registro central (fonte única). | Manter só `free/pro/enterprise` (decisão 008); mapear plano→módulo com `if` espalhado no código | 5 tiers refletem melhor a operação real e permitem venda incremental; registro central evita duplicidade/drift entre front e backend; RLS garante que o gating não é só cosmético | Ativa | [ADR-005](../docs/08_DECISOES/adr-005.md) |
| 021 | 2026-07-06 | **Add-ons pagos (decisão 019) são um eixo ortogonal ao plano**, não um módulo "invisível" de um plano especial — módulos efetivos de um tenant = módulos do plano ∪ módulos dos add-ons ativos (tabelas `addons`/`tenant_addons` separadas de `planos_modulos`). | Colocar add-ons dentro da tabela de planos como linhas especiais | Mistura duas linhas de receita (assinatura recorrente vs. contratação avulsa) e dificulta relatório de receita separado por produto | Ativa | [ADR-005](../docs/08_DECISOES/adr-005.md) |
| 022 | 2026-07-06 | **Billing: mensalidade do plano (`assinaturas`) é separada de add-ons pagos** — ciclo de 30 dias, com aviso pré-vencimento, carência e bloqueio ao esgotar a carência sem renovação. Enforcement real mora no backend (`assinatura_ativa(tenant_id)` como cláusula adicional nas políticas RLS), nunca só no front. Renovação nesta fase é **manual** (confirmação de pagamento fora do sistema via RPC); gateway de pagamento pago fica adiado por custo, com o ponto de extensão já pronto (trocar confirmação manual por webhook). Reaproveita o padrão de "marcar vencidos" já usado no Financeiro (decisão 016). | Checagem de vigência só no front; integrar gateway de pagamento já nesta fase; bloqueio único cobrindo plano + add-ons juntos | Segurança real exige backend; gateway pago não se justifica com a base de clientes atual (Restrições de Custo); inadimplência de add-on avulso não deveria travar o sistema inteiro | Revisada (ver 024 — questões em aberto resolvidas) | [ADR-006](../docs/08_DECISOES/adr-006.md) |
| 023 | 2026-07-06 | **Theming/white-label via CSS Custom Properties** (`--gm-*`), sobrescritas por tenant a partir de `tenants.tema` (jsonb), injetadas em runtime — não CSS-in-JS nem build por tenant (Tailwind config por tenant exigiria N builds, contradiz "um único codebase"). Padrão de separação CSS/JSX (decisão 018) fixado como arquivo `.css` puro co-localizado (mesmo nome do componente), não CSS Modules nem styled-components — sem dependência nova, sem mudança de build. | CSS-in-JS com ThemeProvider; Tailwind config customizado por build/tenant; CSS Modules | Custom properties resolvem tema em runtime sem rebuild, condição necessária para "um único codebase, N tenants"; `.css` puro colocalizado não exige nova dependência nem muda o pipeline do Vite | Ativa | [ADR-007](../docs/08_DECISOES/adr-007.md) |
| 024 | 2026-07-06 | **Resolução das 3 questões em aberto do ADR-006 (billing):** (1) bloqueio ao vencer a assinatura é **total** — impede tudo, inclusive login/leitura e fechamento de caixa aberto, sem exceção operacional; (2) carência padrão = **3 dias** (`assinaturas.carencia_dias DEFAULT 3`); (3) o status de enforcement é **derivado na consulta** a partir de `data_vencimento`/`carencia_dias` via função SQL pura (`calcular_status_assinatura`), nunca dependente de job ter rodado — `pg_cron`/checagem lazy no bootstrap servem só para manter um cache de status (UI/relatórios), nunca para decidir o bloqueio. Nesta fase, a sincronização do cache é **lazy no bootstrap**, sem `pg_cron`. | Bloqueio parcial (permitir leitura/fechar caixa); carência de 5 dias; status armazenado em coluna atualizada só por job agendado | Bloqueio total é mais simples e sem ambiguidade; 3 dias é o prazo de carência decidido pelo founder; calcular na consulta evita que a segurança dependa de um job (pago ou não) ter executado — a primeira escrita após vencimento+carência já falha corretamente mesmo sem nenhuma sincronização prévia | Ativa | [ADR-006](../docs/08_DECISOES/adr-006.md) |
| 026 | 2026-07-10 | **Credenciais fiscais por tenant, em branco por padrão** — certificado A1, senha do certificado, CSC, IE e token do provedor fiscal são campos POR ESTABELECIMENTO (por tenant), preenchidos no onboarding, nunca hardcodados. **Segurança:** certificado e senhas NUNCA vão ao frontend — ficam server-side e a emissão de nota passa por Edge Function (mesmo padrão de `jarvas-assistente`). | Credenciais fiscais globais/únicas da plataforma; guardar no frontend | Cada cliente emite com o próprio CNPJ/certificado; segredos fiscais são sensíveis e só podem viver server-side | Ativa | [ADR-008](../docs/08_DECISOES/adr-008.md) |
| 027 | 2026-07-10 | **Dois níveis de admin** — (a) **Console da plataforma** (super-admin / dono do SaaS): login próprio, papel cross-tenant, cria estabelecimentos, define planos/add-ons, concede assinatura, vê billing de todos; (b) **Configurações do estabelecimento** (admin do tenant): escopado ao próprio tenant (tema/logo, usuários, perfil de impressão, ver própria assinatura). | Um único papel admin com "poderes extras"; back-office cross-tenant misturado à operação do estabelecimento | Vender SaaS exige um back-office da plataforma separado e mais protegido, distinto da administração que o dono do restaurante faz no próprio tenant | Ativa | [ADR-008](../docs/08_DECISOES/adr-008.md) |
| 028 | 2026-07-11 | **Isolamento multi-tenant real + Console da plataforma (realiza a decisão 002)** — adiciona `tenant_id` a `users` e a todas as tabelas de dado de negócio + RLS de isolamento por tenant; injeta `app_metadata.tenant_id` no hook JWT; cria helpers `auth.tenant_id()`/`auth.is_super_admin()`; troca só o corpo dos wrappers `tenant_atual_tem_modulo`/`assinatura_atual_ativa` (as 48 policies de assinatura e as de módulo ficam intactas — isolamento é aditivo). Super-admin = papel `plataforma` + `tenant_id` nulo; acesso cross-tenant restrito (impersonation + `OR is_super_admin()` só em billing/listagem); Console como rota no mesmo app. | Schema/banco por tenant; `OR is_super_admin()` global; Console em deploy separado; reescrever as 48 policies | Bloqueador nº 1 da venda multi-estabelecimento (decisão 017); desenho aditivo aproveitando as Fases 1-5 já prontas | Aceita | [ADR-008](../docs/08_DECISOES/adr-008.md) |
| 029 | 2026-07-16 | **Tabela de preços dos planos (decisão do dono)** — Básico **R$ 149**/mês · Simples **R$ 249** · Médio **R$ 349** · Alto **R$ 497** · Avançado **R$ 1.397**; add-ons: NF-e **R$ 79**/mês · TEF **R$ 69**/mês. Posicionamento: tiers Básico→Alto na régua do mercado de PDV independente (Anota AI/Goomer R$ 99–225, Saipos R$ 240+, Consumer ~R$ 300, média do setor R$ 100–500); **Avançado é tier premium deliberado** — IA (Jarvas), multi-loja e white-label são para rede/franquia/alto volume, comprador com régua de ERP (Linx/Teknisa R$ 1.000+), venda consultiva ("fale com a gente"), não autosserviço. Alertas simples ficam no Médio (o pequeno não fica no escuro); insight inteligente é o motivo de subir ao topo. Primeiros clientes (GastroMundi, Casa Coffee) entram como **preço fundador** (desconto forte em troca de case), preço cheio na tabela pública. Preços ainda NÃO gravados no banco (`planos` não tem coluna de preço; `assinaturas.valor_mensal` segue manual por tenant) — gravar `preco_mensal` no catálogo quando a vitrine/Console for exibi-los. | Entrar ~20% abaixo dos incumbentes em todos os tiers; teto de R$ 499 no Avançado | White-label + IA não existem nos concorrentes da faixa; precificar IA barato desvaloriza o diferencial; o salto Alto→Avançado (R$ 497→1.397) é ancoragem good-better-best proposital e segmenta independente vs. rede | Ativa | — |

## Decisões em Aberto

| Tema | Status | Notas |
|------|--------|-------|
| Biblioteca de data-fetching/cache | Resolvida (ver 013) | TanStack Query via hooks gerados pelo Orval |
| Ferramenta de testes | Em avaliação | Vitest + Testing Library (provável) |
| Estratégia de SSR | Adiada | Reavaliar se SEO se tornar crítico (ver ADR-001) |
