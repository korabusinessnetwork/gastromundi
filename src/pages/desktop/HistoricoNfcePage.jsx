import HistoricoNfce from "@/components/fiscal/HistoricoNfce";

/**
 * Página de gestão — histórico das NFC-e emitidas (Leva 12). Consulta do
 * gestor/operador (reimpressão + cancelamento), fora do fluxo do caixa.
 */
export default function HistoricoNfcePage() {
  return <HistoricoNfce />;
}
