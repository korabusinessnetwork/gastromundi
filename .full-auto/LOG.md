# Log de execução

<!-- uma linha por tarefa: data · id · resultado · commit -->

- 2026-09-10 · T01 · ADR-013 escrito (PDV offline-first: outbox, dreno FIFO, carimbo de tenant, snapshot, os 8 tipos de op e 6 pendências nomeadas), indexado no `overview.md`, linha do F021 atualizada no `features.md`. Suíte 229 arquivos / 4067 testes verdes.
- 2026-09-10 · T02 · TD009 etapa 3 fechada: nenhum caminho do app grava em `sales`, `persistirVendaNormalizada` devolve desfecho em vez de engolir erro, cancelamento virou coluna em `vendas` (migration 20260920) em vez de apagar as filhas, e `jaExistia` fechou a pendência 6 do ADR-013. Review aprovada sem ressalvas nos 11 critérios. Suíte 230 arquivos / 4082 testes verdes. Commit `6e5523cd`.
- 2026-09-10 · T03 · TD008 fechado: o contador de tentativas de login saiu do `localStorage` e virou `public.login_tentativas` (migration 20260927), com RLS ligada, sem policy e três funções `SECURITY DEFINER`, sendo que a que zera o contador não recebe chave por parâmetro e só o `authenticated` executa. `login` consulta o servidor antes do `signInWithPassword`; o contador local virou eco para os pips. Falha aberta de propósito quando a RPC não responde. Review aprovada sem ressalvas nos 11 critérios. Suíte 232 arquivos / 4102 testes verdes. Commit `cbe52747`.
- 2026-09-10 · T04 · TD015 fechado: as 40 ocorrências de `key={i}` viraram 10 chaves de domínio, 11 `uid` de `src/lib/uidLista.js` (novo, com 16 testes) e 19 índices mantidos com justificativa escrita no código. A review pegou um furo silencioso, a chave dos fornecedores do `ProdutosView` estava escrita como `c.uid ?? idx` sem ninguém carimbar `uid`, e caía sempre no índice; corrigido nos dois pontos onde a linha nasce. Nenhum `uid` chega ao banco, verificado save a save. Review aprovada sem ressalvas nos 11 critérios. Suíte 232 arquivos / 4102 testes para 233 arquivos / 4118 testes, verdes.
- 2026-09-10 · T05 · F021 fatia 2 fechada: a fila offline saiu do `localStorage` e passou a morar no IndexedDB, com o banco EMBAIXO de um espelho síncrono em memória (`src/lib/offline/storageIdb.js`, novo, 26 testes), porque a API da fila é síncrona e dois `useState` preguiçosos dependem disso. `fila.js` e `fila.test.js` não mudaram uma linha. A hidratação mescla por `uid` em vez de sobrescrever, o legado do `localStorage` só é apagado depois de o banco confirmar, e sem IDB degrada para memória com `{ idb: false }` declarado. A review pegou 8 testes de integração acoplados ao `localStorage` e o critério 11 sem teste nenhum; os dois corrigidos. Review aprovada sem ressalvas nos 14 critérios. Suíte 233 arquivos / 4118 testes para 235 arquivos / 4148 testes, verdes, `npm run build` limpo.
- 2026-09-11 · T06 · F018 fatia 11: primeira fatia do `NotasFiscaisTab.jsx`, o arquivo que assumiu o topo da lista de estilo inline depois que o `PDVView` e o `DeliveryView` fecharam. Saíram as quatro regiões que não são formulário (o `Stepper`, a linha de vínculo do step 3, a tela de Lista e a de Detalhe): 195 → **124** `style={{` no arquivo e zero no trecho, com o total do `src` caindo de 1600 para **1529**. Junto foram os 8 `currentTarget.style` das quatro regiões, que viraram `:hover` de verdade, e os dois inputs da linha de vínculo passaram a receber o foco do `inputs.css` global. Os três estados do `Stepper` viraram um modificador no item em vez de cinco ternários de cor. A migração rodou por script com contagem declarada por substituição, 22 substituições esperando 1 ocorrência cada, e gravou de primeira. A review pegou a conta errada do próprio critério 3 da spec (dizia 12 `currentTarget.style` na fatia, são 8) e corrigiu a spec, não o número. Aprovado sem ressalvas nos 11 critérios. Suíte 235 arquivos / 4148 testes verdes, `npm run build` limpo.
